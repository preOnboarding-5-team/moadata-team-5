interface UserInfo {
  id: number;
  registerDate: string;
  loginId: string;
  password: string;
}

interface UserDataType {
  userId: string;
  userNumber: string;
  prevDate?: string;
  nextDate?: string;
}
